import 'package:flutter/material.dart';

// class AppColors {
//   AppColors._();

//   // ─── Backgrounds ─────────────────────────────────────────────────────────────
//   static const backgroundDark  = Color(0xFF050D1A);
//   static const backgroundMid   = Color(0xFF080F1E);
//   static const sidebarBg       = Color(0xFF060D1C);
//   static const cardBackground  = Color(0xFF0C1A2E);
//   static const cardBorder      = Color(0xFF162035);
//   static const activeNavBg     = Color(0xFF0D2040);

//   // ─── Input Fields ─────────────────────────────────────────────────────────────
//   static const inputBackground = Color(0xFF0D1B2E);
//   static const inputBorder     = Color(0xFF1C3050);

//   // ─── Primary ──────────────────────────────────────────────────────────────────
//   static const primaryCyan     = Color(0xFF00D4FF);
//   static const primaryBlue     = Color(0xFF1A6FFF);
//   static const gradientStart   = Color(0xFF00D4FF);
//   static const gradientEnd     = Color(0xFF1A6FFF);

//   // ─── Accents ──────────────────────────────────────────────────────────────────
//   static const accentGreen     = Color(0xFF00C48C);
//   static const accentOrange    = Color(0xFFFFB020);
//   static const accentPurple    = Color(0xFF9B8AFB);
//   static const accentRed       = Color(0xFFFF4D6A);

//   // ─── Text ─────────────────────────────────────────────────────────────────────
//   static const textWhite       = Color(0xFFFFFFFF);
//   static const textGray        = Color(0xFF8A9BB5);
//   static const textSubtle      = Color(0xFF3D5068);

//   // ─── Chart ────────────────────────────────────────────────────────────────────
//   static const chartCyan       = Color(0xFF00D4FF);
//   static const chartBlue       = Color(0xFF1A6FFF);
//   static const chartGreen      = Color(0xFF00C48C);
//   static const chartOrange     = Color(0xFFFFB020);
//   static const chartPurple     = Color(0xFF9B8AFB);
//   static const chartGray       = Color(0xFF3D5068);
// }



// ============ For light Mode ============================

class AppColors {
  AppColors._();

  // ─── Backgrounds ─────────────────────────────────────────────────────────────
  static const backgroundDark  = Color(0xFFF5F7FA);  // Light gray background
  static const backgroundMid   = Color(0xFFEBF0F5);  // Soft gray-blue
  static const sidebarBg       = Color(0xFFF8F9FC);  // Very light sidebar
  static const cardBackground  = Color(0xFFFFFFFF);  // Pure white cards
  static const cardBorder      = Color(0xFFE4E9F0);  // Subtle border
  static const activeNavBg     = Color(0xFFE3F2FD);  // Light blue active state

  // ─── Input Fields ─────────────────────────────────────────────────────────────
  static const inputBackground = Color(0xFFF8F9FA);  // Light input background
  static const inputBorder     = Color(0xFFD1D9E6);  // Soft border

  // ─── Primary ──────────────────────────────────────────────────────────────────
  static const primaryCyan     = Color(0xFF00B4D8);  // Softer cyan
  static const primaryBlue     = Color(0xFF3D8AFF);  // Lighter blue
  static const gradientStart   = Color(0xFF5E9FFF);  // Softer gradient start
  static const gradientEnd     = Color(0xFF3D8AFF);  // Softer gradient end

  // ─── Accents ──────────────────────────────────────────────────────────────────
  static const accentGreen     = Color(0xFF5CB85C);  // Soft green
  static const accentOrange    = Color(0xFFF4A261);  // Warm orange
  static const accentPurple    = Color(0xFFA78BFA);  // Soft purple
  static const accentRed       = Color(0xFFEF476F);  // Muted red

  // ─── Text ─────────────────────────────────────────────────────────────────────
  static const textWhite       = Color(0xFF1A1A2E);  // Dark text (was white)
  static const textGray        = Color(0xFF546E7A);  // Medium gray text
  static const textSubtle      = Color(0xFF90A4AE);  // Light gray text

  // ─── Chart ────────────────────────────────────────────────────────────────────
  static const chartCyan       = Color(0xFF00B4D8);  // Softer chart cyan
  static const chartBlue       = Color(0xFF3D8AFF);  // Softer chart blue
  static const chartGreen      = Color(0xFF5CB85C);  // Softer chart green
  static const chartOrange     = Color(0xFFF4A261);  // Softer chart orange
  static const chartPurple     = Color(0xFFA78BFA);  // Softer chart purple
  static const chartGray       = Color(0xFFB0BEC5);  // Light chart gray
}